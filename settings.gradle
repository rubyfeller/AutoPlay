pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "AutoPlay"
include ':app'
include ':spotify-app-remote'
project(':spotify-app-remote').projectDir = new File('app/libs/spotify-app-remote-release-0.7.2.aar')
